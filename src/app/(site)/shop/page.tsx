import { ShopClient } from "@/components/shop/ShopClient";

export default function ShopPage() {
  return (
    <ShopClient
      title="Каталог"
      description="Все товары в одном месте — используйте поиск и фильтры, чтобы найти нужное."
    />
  );
}
